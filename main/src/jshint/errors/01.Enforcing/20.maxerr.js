/*
 * maxerr
 * http://jshint.com/docs/options/#maxerr
 * This options allows you to set the maximum amount of warnings JSHint will produce before giving up.
 * Default is 50.
 * */
function maxerr(arg) {
  var greeting = 'hello world!'
  console.log(greeting);

  var empty = {};

  return arg + someArg;
}

maxerr();