/*
 * maxparams
 * http://jshint.com/docs/options/#maxparams
 * This option lets you set the max number of formal parameters allowed per function.
 * */
function maxparams(arg1, arg2, arg3) {
  return arg1 + arg2 + arg3;
}

maxparams(1, 2, 3);